version: 2.1

workflows:
  version: 2
  build_pipeline:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.11
          docker_layer_caching: true
      - run:
          command: |
            docker build -t regviz/node-xcb:16 .
            echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
            docker push regviz/node-xcb:16
